namespace Cella.Storage.Objects;

public enum IndexType
{
    Heap,
    Clustered,
    NonClustered,
    Object,
    Spatial,
    ClusteredColumnStore,
    NonClusteredColumnStore,
    NonClusteredHash
}